# Testing dependencies for AIND Dashboard
# Essential testing tools only to avoid compatibility issues

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0

# For HTTP testing in smoke tests
requests>=2.25.0

# Additional testing utilities (minimal set)
factory-boy>=3.2.0
freezegun>=1.2.0

# Development and debugging
pytest-xdist>=3.0.0 